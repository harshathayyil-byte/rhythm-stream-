{
  "name": "rhythm-stream",
  "version": "1.0.0",
  "description": "Rhythem Stream - Fullstack Music Streaming Platform",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8000 --ssl-keyfile ../key.pem --ssl-certfile ../cert.pem",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
